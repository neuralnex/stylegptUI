import { heroui } from "@heroui/theme";

// HeroUI theme config entry for Tailwind v4 setups.
// Tailwind v4 consumes this file when you import it in your CSS or build pipeline.
export default heroui();

